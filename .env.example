# ==============================================================================
# LLM & CLAUDE API CONFIGURATION
# ==============================================================================
ANTHROPIC_MODEL=claude-opus-4-7
TEMPERATURE=0.3

# ==============================================================================
# STREAMLIT CONFIGURATION
# ==============================================================================
STREAMLIT_PORT=8501
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_LOGGER_LEVEL=info

# ==============================================================================
# SHIELDPROMPT CONFIGURATION
# ==============================================================================
DETECTION_THRESHOLD=0.85
AUTO_REMEDIATION_ENABLED=true
ESCALATION_THRESHOLD=0.95
LOG_LEVEL=INFO

# ==============================================================================
# DATABASE / STORAGE
# ==============================================================================
DATA_PATH=./data
LOG_PATH=./logs

# ==============================================================================
# THREAT INTELLIGENCE
# ==============================================================================
# Optional static dataset of threat patterns to ingest on startup (JSON list or
# dict of records). Leave blank to rely on mock patterns / snapshots.
DATASET_PATH=

# ==============================================================================
# TIER 2: LLM SEMANTIC ANALYSIS
# ==============================================================================
ANTHROPIC_API_KEY=          # Claude Haiku — LLMEvaluator (~$0.001/scan)
OPENAI_API_KEY=             # text-embedding-3-small — VectorStore (~$0.00002/scan)
ENABLE_TIER2_SEMANTIC=true  # Master flag for Tier 2 (default: true)
LLM_MODEL=claude-haiku-4-5-20251001
EMBEDDING_MODEL=text-embedding-3-small

# Dev/offline mode: seed built-in mock patterns (no external keys needed)
USE_MOCK_PATTERNS=false

# ==============================================================================
# SNAPSHOTS / INTEL STORE  (see docs/INTEL.md)
# ==============================================================================
# Frozen intel bundles are written here and committed to git for portability.
SNAPSHOT_DIR=./snapshots
# Auto-load the warm cache (./data) on startup so the pattern set persists.
AUTO_LOAD_ON_INIT=true
# Promote high/critical pattern regexes into Tier 1 (needs no keys).
ENABLE_DYNAMIC_SIGNATURES=true
DYNAMIC_SIGNATURE_MIN_SEVERITY=high
